{
  "name": "crowdfund",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy-testnet": "npx hardhat run ./scripts/BlankToken_deploy.js --network ETH_GOERLI",
    "deploy": "npx hardhat run ./scripts/BlankToken_deploy.js --network ETH_MAINNET",
    "node": "npx hardhat node",
    "deploy-local": "npx hardhat run ./scripts/BlankToken_deploy.js --network localhost",
    "lint": "prettier --list-different 'contracts/**/*.sol'",
    "prettier": "prettier --write 'contracts/**/*.sol'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.14.0",
    "prettier": "^2.8.8",
    "prettier-plugin-solidity": "^1.1.3"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.1",
    "@openzeppelin/contracts": "^4.8.3",
    "@poanet/solidity-flattener": "^3.0.9"
  }
}
